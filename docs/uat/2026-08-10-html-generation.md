# 2026-08-10 08:00提前版HTML生成与UAT记录

## 范围

- 内容源：`reports/2026-08-10-0800-premarket-news-ranking.md`（主工作区）
- 固定模板分支：`codex/news-radar-web`
- 结果页：`web/dist/reports/2026-08-10-0800.html`
- 本轮生成、验收并发布到独立公开站点仓库；不修改自动任务。

## 设计与实现

- 继续采用已确认的B方案“研究终端”；按反馈最小调整固定模板交互，只有深度内容的Top 10保留展开按钮，只有来源的紧凑条目直接显示来源。
- 新报告使用标准文件名，由目录发现器自动加入报告索引。
- 发现新Markdown采用加粗字段 `新闻窗口：` 和 `实际截点：`，旧解析器不能完整识别。
- 先增加真实解析测试并分别观察窗口、截点断言失败，再最小扩展 `web/report_parser.py`；旧格式分支保持不变。
- 在隔离工作树中运行模板代码，读取主工作区的最新Markdown，并原子重建 `web/dist/`。

## RED → GREEN

1. `test_standard_report_keeps_explicit_window_and_actual_cutoff` 首次运行：1项失败，实际截点为空。
2. 增加实际截点解析后：该断言通过。
3. 将同一契约扩展至加粗新闻窗口：首次运行1项失败，窗口为空。
4. 增加加粗新闻窗口解析后：该测试通过。
5. 第一轮全套：51项通过，0失败，0错误，0跳过。
6. 独立复核发现源稿第10条使用 `财报市场反馈：`，解析器未识别，页面遗漏该段。
7. 增加真实标签断言并观察1项失败；最小增加标签兼容后，聚焦测试和51项全套再次通过。
8. 对“仅来源条目直接显示来源、无展开按钮”增加渲染契约；首次失败时仍输出普通来源详情类型，修复后全套增至53项并全部通过。

## 构建结果

- 报告数：6。
- 新闻条目总数：145。
- 最新入口：`reports/2026-08-10-0800.html`。
- 8月10日报告：25条，实际截点 `2026-08-09 19:44:27`。
- 8月10日页面具体外部来源链接：38个。

## 浏览器UAT

### 桌面端

- 最新报告标题、25条新闻、96至63分排序显示正常。
- 左侧报告归档共6份，可从8月10日切换到8月3日并正确标记当前报告。
- 第1条详情可展开，来源链接指向具体财联社文章。
- “财报”筛选后仅保留4条财报分类，按钮状态为已选择。
- 数据截止显示为 `2026-08-09 19:44:27`。
- 第10条伯克希尔详情已显示“周六发布，下一可交易时段为8月10日美股；即时市场定价不可得”。

### 移动端

- 390×844视口下固定侧栏隐藏，顶部报告选择器显示。
- 页面宽度与滚动宽度均为390px，无横向滚动。
- 下拉选择8月3日报告后页面和选中项均正确更新。

### 运行时

- 加载1份CSS、1份JavaScript。
- 浏览器控制台：0条警告，0条错误。
- 直接 `file://` 预览被浏览器安全策略拒绝；改用仅绑定 `127.0.0.1` 的临时静态服务完成验收。

## 安全与发布边界

- `assert_public_tree_safe(web/dist)` 通过。
- 结果页不包含 `/Users/` 本机绝对路径、Cookie、Local Storage或证据目录内容。
- `git diff --check` 通过。
- 发布前对独立公开仓库执行 dry-run，预期同步10个静态文件；自动任务未修改。
- 静态目录已同步到独立公开仓库 `xrqaviva/market-news-site`，提交为 `4150b69fa1b0c60e568ac4517d4dec96e3602e4a`。
- GitHub Pages发布源为 `main` 分支根目录，公开访问且强制HTTPS；Pages构建状态为 `built`、错误为空。
- 首页 `https://xrqaviva.github.io/market-news-site/` 与最新报告 `https://xrqaviva.github.io/market-news-site/reports/2026-08-10-0800.html` 均返回HTTP 200。

## 独立只读复核

- 复核者：内部只读代码审阅代理 `html_readonly_review`。
- 初次结论：1个阻断性市场反馈字段丢失；其他25条、排序、截点、38个条目来源链接、6期切换和安全扫描通过。
- 阻断项已按RED → GREEN修复并在浏览器复验，控制台仍为0警告、0错误。
- 非阻断说明：第20、25条源Markdown本身使用财联社投资日历首页；生成器没有改写链接。页面保持紧凑研究终端契约，报告顶部完整方法与来源限制仍保留在Markdown，不在结果页重复展开。
- 最终只读复核无阻断项：Top 10交互、11—25直接来源、六期历史页面、公开树安全扫描和53项测试全部通过。非阻断建议是后续增加JavaScript DOM自动化回归测试，当前由渲染契约、脚本检查和浏览器UAT共同覆盖。

## 用户截图反馈修复

### 问题

- 第11—25条为紧凑条目，没有Top 10深度字段，但统一显示“展开详情”，展开后只有来源链接，语义与内容不符。
- 紧凑摘要解析使用普通Markdown纯文本转换，导致链接标题被拼入摘要末尾，出现孤立的“具体汇总”“具体稿”“投资日历”。
- 紧凑来源元信息统一显示“报告链接”，来源名称过于泛化。

### RED → GREEN

- 新增解析测试：紧凑摘要必须移除Markdown链接文字，同时保留完整来源对象；首次失败，摘要包含来源标题。
- 新增渲染测试：来源型条目与深度型条目必须输出不同的 `data-detail-kind`；首次失败，页面没有该语义。
- 最小修复后两项聚焦测试通过；全套测试增至53项。

### 内容与交互

- 第11—25条摘要不再显示链接标题。
- 第11—25条仅含来源时直接在摘要下方显示来源，不渲染按钮或折叠容器；Top 10仍为“展开详情/收起详情”。
- 紧凑来源元信息由“报告链接”改为“原始来源”。
- 8月10日报告的泛化链接已改为包含媒体、栏目和核验主题的完整名称，例如：
  - `财联社周末要闻汇总：茅台直营店价格`；
  - `财联社投资日历：MSCI中国全股票指数调整`；
  - `财联社美股盘前汇总：Terafab项目`。
- 浏览器复验：Top 10共有10个展开按钮；第11—25条共有15个直接可见的来源且按钮数为0；第24、25条来源名称完整，第24条链接仍为原财联社具体汇总页；控制台0警告、0错误。
