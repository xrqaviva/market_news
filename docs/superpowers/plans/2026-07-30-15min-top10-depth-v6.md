# 15分钟盘前新闻深度V6试跑 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在正常来源可用条件下，用3个sub-agent并行采集，在15分钟内生成一份前10条含深度信息、其余条目保持紧凑的盘前新闻V6试跑报告。

**Architecture:** 主任务固定北京时间窗口并负责去重、排序、写作和验收；海外宏观、国内政策公司、登录渠道热度三个sub-agent分工采集。约第7分钟冻结初排前10，随后并行补充预期差、市场反馈、判断边界与后续变量。

**Tech Stack:** Markdown报告与证据表、公开官方/媒体页面、登录Chrome可见字段、Python只读结构验收。

## Global Constraints

- 正常条件下从正式采集开始到报告完成不超过15分钟，并记录各阶段时间。
- 新闻窗口继续从前一交易日00:00北京时间开始，截点为本次试跑结束前的最后采集时点。
- 海外时间换算为Asia/Shanghai，保留原时区；未取得时明确写明。
- 排序仍为覆盖25 + 变化30 + 绝对热度20 + 新鲜度15 + 频次10；深度信息不参与排序。
- 前10每条含关键信号/预期差、市场反馈、判断边界、后续关键变量；第11名以后保持方案A紧凑结构。
- 只有热度sub-agent操作登录Chrome；不读取密码、Cookie、Local Storage或浏览器历史。
- 无直接证据时不用确定因果措辞，不提供交易建议。
- 试跑创建V6新文件，不覆盖V5。
- 当前目录不是Git仓库，提交、推送、PR和部署均为N/A。

---

### Task 1: 启动计时与建立V6证据骨架

**Files:**
- Create: `evidence/2026-07-30-premarket-depth-v6-snapshots.md`
- Modify: `progress.md`

**Interfaces:**
- Consumes: `docs/superpowers/specs/2026-07-30-15min-top10-depth-design.md`
- Produces: 正式开始时间、窗口、分阶段时间字段和候选事件模板。

- [x] **Step 1: 运行RED证据验收**

  Run: `python3 -c 'from pathlib import Path; p=Path("evidence/2026-07-30-premarket-depth-v6-snapshots.md"); assert p.exists() and "正式采集开始" in p.read_text()'`

  Expected: FAIL because the V6 evidence file does not exist.

- [x] **Step 2: 记录正式采集开始时间并建立证据表头**

  写入北京时间开始时刻、前一交易日、主窗口起点、初排完成/深挖完成/报告完成占位字段的明确格式；时间未发生前写“尚未发生”，不伪造时刻。

- [x] **Step 3: 运行GREEN证据验收**

  Run Step 1 again.

  Expected: PASS with exit code 0.

### Task 2: 三路并行采集与第7分钟初排

**Files:**
- Modify: `evidence/2026-07-30-premarket-depth-v6-snapshots.md`
- Modify: `findings.md`

**Interfaces:**
- Consumes: V5候选、上一窗口热度基线和三个sub-agent的结构化返回。
- Produces: 去重候选、北京时间时间链、具体链接、热度快照、初排分数和前10事件ID。

- [x] **Step 1: 并行派发三个只读采集任务**

  海外组返回宏观/财报/地缘事件及跨资产反馈；国内组返回政策/产业/公告；热度组独占Chrome返回微博/雪球/X/NewsNow热度与变化。各组不得写共享报告文件。

- [x] **Step 2: 合并事件并按北京时间去重**

  同一事实的多平台传播合并为一个事件；事件发生、媒体发布时间、首次发现和热度快照分开保存。

- [x] **Step 3: 计算初排并记录冻结时间**

  计算五项热度分、严格降序，记录前10事件及初排完成时刻；深度字段不得进入分数。

- [x] **Step 4: 验证初排候选**

  Run: `python3 -c 'from pathlib import Path; s=Path("evidence/2026-07-30-premarket-depth-v6-snapshots.md").read_text(); assert s.count("## EVT-") >= 20; assert "前10初排" in s and "初排完成" in s'`

  Expected: PASS with at least 20 events or an explicit checked-source explanation if fewer qualify.

### Task 3: 前10深挖与V6报告生成

**Files:**
- Create: `reports/2026-07-30-premarket-news-ranking-v6-depth-test.md`
- Modify: `evidence/2026-07-30-premarket-depth-v6-snapshots.md`

**Interfaces:**
- Consumes: 冻结前10及三组第二阶段深度结果。
- Produces: 前10深度块、第11名以后紧凑条目、表内可点击永久链接。

- [x] **Step 1: 运行RED报告验收**

  Run: `python3 -c 'from pathlib import Path; p=Path("reports/2026-07-30-premarket-news-ranking-v6-depth-test.md"); s=p.read_text() if p.exists() else ""; assert s.count("**关键信号/预期差：**") == 10'`

  Expected: FAIL because the V6 report does not exist.

- [x] **Step 2: 对前10并行补齐四段深度信息**

  每条写关键信号/预期差、带时间和链接的市场反馈、事实/解读/时序边界、后续关键变量；缺数据时明确写未取得。

- [x] **Step 3: 生成完整V6报告**

  前10采用深度结构，第11名以后保留标题、分数、核心信息、传播时间表和紧凑尾注；所有具体来源链接嵌入相应详情文字。

- [x] **Step 4: 记录深挖和报告完成时间**

  在证据表写入深挖完成、报告完成和总耗时；总耗时按正式开始至报告完成计算。

- [x] **Step 5: 运行GREEN报告验收**

  Run Step 1 again.

  Expected: PASS with exactly 10 depth blocks.

### Task 4: 独立审阅与最终验证

**Files:**
- Modify: `reports/2026-07-30-premarket-news-ranking-v6-depth-test.md`
- Modify: `evidence/2026-07-30-premarket-depth-v6-snapshots.md`
- Modify: `progress.md`

**Interfaces:**
- Consumes: 完整V6报告、证据表和计时数据。
- Produces: 性能、结构、语义、链接和安全边界的最终结果。

- [x] **Step 1: 运行完整结构与计时验收**

  检查事件数、10个深度块、分数加总/降序、北京时间、永久链接、总耗时和长尾无强制深度块。

- [x] **Step 2: 执行金融内容独立只读审阅**

  重点审阅预期差依据、市场反馈时间、因果措辞、后续变量、旧闻准入和链接可回溯性。

- [x] **Step 3: 修正阻断发现并重跑验收**

  对每个阻断问题建立定向断言，修正后重跑定向检查和完整验收。

- [x] **Step 4: 记录试跑结果**

  在`progress.md`记录总耗时、各阶段耗时、条目数、链接数、通过/失败项、超时来源和降级情况。

## Test Matrix

- Unit: N/A，本轮无业务函数；以分数加总、深度块计数和时间解析断言替代。
- Integration: 三组结果合并到同一证据表和报告，事件ID、时间、分数与链接一一对应。
- UI: N/A，交付物为Markdown；Chrome只读采集由热度组负责并在结束时清理其打开标签。
- Security/privacy: 不读取或输出密码、Cookie、Local Storage、历史；sub-agent只返回页面可见字段。
- Generated artifacts: 证据表生成V6报告；检查旧V5未被修改、V6不存在陈旧开始/完成时间。
- Build/compile/lint: N/A，无代码构建；使用Python结构/语义验收和独立只读审阅。
- Full baseline: V5已通过15项结构验收；V6 RED分别因新证据文件和新报告不存在而合理失败。

## Authority and Preservation

- 用户已明确授权本次生成和sub-agent并行。
- 只创建V6报告/证据并更新项目记录，不覆盖V5或用户其他文件。
- 不创建自动化、不对外发布、不执行交易或其他外部状态变更。
